{
  "name": "emojistore",
  "version": "1.0.0",
  "description": "Emoji Store using react, resend, stripe, mongodb",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon --trace-warnings --env-file=.env server.js"
  },
  "author": "Jason Turner",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
